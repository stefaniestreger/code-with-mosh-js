// Create a function that returns the sume of multiples
// of 3 and 5 within the passed limit

console.log(sum(25));

function sum(limit) {
    let sum = 0;

    for (let i = 0; i <= limit; i++)
        if (i % 3 === 0 || i % 5 === 0) 
            sum += i;

    return sum;
}
